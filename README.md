# Lab-Fortigate
A comprehensive security implementation using FortiGate in PNETLab. Features include network segmentation (/25 &amp; /28), DHCP, and static routing. Security enforcement via Web Filter (domain blocking), Application Control (social media), IPS (scanner detection), and WAF protection for web servers. Full traffic logging and policy-based access control.
